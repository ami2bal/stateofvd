# Blueprint rooms (world.json aligned)

Grid 38×24 tiles @ 16px → 608×384px

## parlement (parlement) 14×9 @ (3,2)
   px: 224×144
   - `plenum-gc` 9×7 tiles → 144×112px — Hémicycle du Grand Conseil
   - `bureau-gc` 5×3 tiles → 80×48px — Bureau du Grand Conseil
   - `commission` 5×3 tiles → 80×48px — Commissions parlementaires
   - `sgc` 5×3 tiles → 80×48px — Secrétariat général du Grand Conseil
   - `pas-perdus` 9×2 tiles → 144×32px — Salle des pas perdus

## chateau (chateau) 12×9 @ (21,2)
   px: 192×144
   - `college-ce` 8×7 tiles → 128×112px — Collège du Conseil d'État
   - `csg` 4×4 tiles → 64×64px — Conférence des secrétaires généraux
   - `chancellerie` 4×5 tiles → 64×80px — Chancellerie d'État

## dep-dits (department) 3×5 @ (2,16)
   px: 48×80
   - `dep-dits-cabinet` 3×1 tiles → 48×16px — Cabinet du chef de département
   - `dep-dits-sg` 3×1 tiles → 48×16px — Secrétariat général
   - `dep-dits-projet` 3×3 tiles → 48×48px — Cellule EMPD

## dep-deiep (department) 3×5 @ (7,16)
   px: 48×80
   - `dep-deiep-cabinet` 3×1 tiles → 48×16px — Cabinet du chef de département
   - `dep-deiep-sg` 3×1 tiles → 48×16px — Secrétariat général
   - `dep-deiep-projet` 3×3 tiles → 48×48px — Cellule EMPD

## dep-def (department) 3×5 @ (12,16)
   px: 48×80
   - `dep-def-cabinet` 3×1 tiles → 48×16px — Cabinet du chef de département
   - `dep-def-sg` 3×1 tiles → 48×16px — Secrétariat général
   - `dep-def-projet` 3×3 tiles → 48×48px — Cellule EMPD

## dep-dsas (department) 3×5 @ (17,16)
   px: 48×80
   - `dep-dsas-cabinet` 3×1 tiles → 48×16px — Cabinet du chef de département
   - `dep-dsas-sg` 3×1 tiles → 48×16px — Secrétariat général
   - `dep-dsas-projet` 3×3 tiles → 48×48px — Cellule EMPD

## dep-dcirh (department) 3×5 @ (22,16)
   px: 48×80
   - `dep-dcirh-cabinet` 3×1 tiles → 48×16px — Cabinet du chef de département
   - `dep-dcirh-sg` 3×1 tiles → 48×16px — Secrétariat général
   - `dep-dcirh-projet` 3×3 tiles → 48×48px — Cellule EMPD

## dep-djes (department) 3×5 @ (27,16)
   px: 48×80
   - `dep-djes-cabinet` 3×1 tiles → 48×16px — Cabinet du chef de département
   - `dep-djes-sg` 3×1 tiles → 48×16px — Secrétariat général
   - `dep-djes-projet` 3×3 tiles → 48×48px — Cellule EMPD

## dep-dfa (department) 3×5 @ (32,16)
   px: 48×80
   - `dep-dfa-cabinet` 3×1 tiles → 48×16px — Cabinet du chef de département
   - `dep-dfa-sg` 3×1 tiles → 48×16px — Secrétariat général
   - `dep-dfa-projet` 3×3 tiles → 48×48px — Cellule EMPD
