# attic/ — code mort / archives

Hors du **path live** de `app.js` (aucun import depuis le boot actuel).

| Dossier | Origine | Ne pas réactiver sans tâche |
|---|---|---|
| `iso/` | Ancien renderer isométrique pré-vectoriel | Remplacé par plan 2D `engine/shapes` + `game/world` |
| `pixel/` | Props pixel-art (D-011 léger) | DA v7 vectorielle (`da_lisse_ref.svg`) |

Conserver pour archéologie. **Ne pas importer** dans le path live sans revue.
